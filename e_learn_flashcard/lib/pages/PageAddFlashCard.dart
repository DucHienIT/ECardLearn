import 'package:flutter/material.dart';
class PageAddFlashCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Thêm Flashcard'),
      ),
      body: Center(
        child: Text('Màn hình thêm Flashcard sẽ ở đây.'),
      ),
    );
  }
}